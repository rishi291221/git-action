name: AWS Authentication using IAM user

on:
    workflow_dispatch:

jobs:

    aws-auth-validation:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v7

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v6
              with:
                aws-access-key-id: ${{AKIATABATNW7QAM7FQNX}}
                aws-secret-access-key: ${{ 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}}
                aws-region: us-east-1

            - name: Verify AWS authentication
              run: |
                aws sts get-caller-identity

            - name: List S3 buckets
              run: |
                aws s3 ls
